{
  "version": 2,
  "builds": [
    {
      "src": "./backend/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "./backend/api/(.*)",
      "dest": "/"
    }
  ],
  "rewrites": [{ "source": "./backend/api/(.*)", "destination": "/api" }]
}
